{
  "name": "irrigation-sizing",
  "version": "1.6.0",
  "description": "Irrigation Sizer — Gestione irrigazione a goccia per vasi da terrazzo",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "fast-check": "^4.1.1"
  }
}
